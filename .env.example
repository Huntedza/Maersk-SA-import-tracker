# SA Inbound Tracker - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Maersk API Configuration
REACT_APP_MAERSK_API_KEY=your_maersk_api_key_here

# API URL Configuration (Optional)
# Leave blank for automatic detection:
# - Development: automatically uses http://localhost:3002/api  
# - Production: automatically uses /api (relative path)
# Only set this if you need to override the automatic detection
# REACT_APP_API_URL=/api

# Application Configuration
REACT_APP_USE_MOCK_DATA=false
REACT_APP_LOG_API_REQUESTS=false

# Node Environment
NODE_ENV=production

# Backend Server Configuration (for production deployment)
PORT=3002

# Instructions for IT Deployment:
# 1. Get a Maersk API key from: https://developer.maersk.com/
# 2. Replace 'your_maersk_api_key_here' with your actual API key
# 3. Keep REACT_APP_API_URL=/api for production (works with Nginx proxy)
# 4. For development: set REACT_APP_USE_MOCK_DATA=true if no API access
# 5. Set REACT_APP_LOG_API_REQUESTS=true to debug API calls
# 6. For production: keep NODE_ENV=production
